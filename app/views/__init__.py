"""
View layer: static assets live in the repo ``ui/`` directory (see :class:`flask.Flask` ``static_folder``
in :func:`app.create_app`). Server-side templates are not used (SPA-style HTML + JSON API).
"""
