#!/usr/bin/env python3
"""Armor Data Anonymizer — entry point (web UI or ``pipeline`` subcommand)."""
from __future__ import annotations

from app.cli import main

if __name__ == "__main__":
    main()
